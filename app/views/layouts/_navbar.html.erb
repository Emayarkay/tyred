<div class="navbar">
  <div class="nav-link"><%= link_to "Garage", bikes_path %></div>
  <% if user_signed_in? %>
    <div class="nav-link"><%= link_to "Profile", user_path(current_user) %></div>
    <div class="nav-link"><%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete} %></div>
  <% else %>
    <div class="nav-link"><%= link_to "Log in", new_user_session_path %></div>
    <div class="nav-link"><%= link_to "Sign up", new_user_registration_path %></div>
  <% end %>
  <div class="nav-link"><%= link_to "Bike Shops", "/shop_locator" %> </div>
</div>
